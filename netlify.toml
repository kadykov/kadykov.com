# Build is handled by GitHub Actions
# Netlify only hosts the pre-built static files
[build]
  publish = "dist"
  # Note: No build command - GitHub Actions builds and deploys

[[redirects]]
  from = "/umami/*"
  to = "https://cloud.umami.is/:splat"
  status = 200
  force = true
